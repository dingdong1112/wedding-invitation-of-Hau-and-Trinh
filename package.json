{
    "dependencies": {
        "esbuild": "^0.25.0",
        "mongodb": "^6.5.0",
        "page-flip": "^2.0.7"
    },
    "scripts": {
        "dev": "npx esbuild js/*.js --bundle --outdir=dist --servedir=. --serve=8081",
        "build": "npx esbuild js/*.js --bundle --minify --outdir=dist --external:page-flip",
        "build:public": "npm run build && rm -rf public && mkdir public && cp -r dist public/ && cp -r assets public/ && cp -r css public/ && cp -r js public/ && cp index.html public/ && cp dashboard.html public/"
    }
}